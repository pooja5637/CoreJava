package com.revision.core.AccessModifiers.pack1;

public class A {
	
 /* If we declared class as public then we can access that class from anywhere, within the package or outside the package.*/
	
}
